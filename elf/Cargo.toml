[package]
name = "elpytios-elf"
version = "0.1.0"
edition = "2024"
rust-version = "1.98"

[dependencies]
bitflags = "2"
bytemuck = { version = "1", features = ["derive", "latest_stable_rust"] }
const_panic = { version = "0.2", features = ["derive", "rust_latest_stable"] }
